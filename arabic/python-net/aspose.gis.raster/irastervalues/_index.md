---
title: "فئة IRasterValues"
type: docs
weight: 30
url: /ar/python-net/aspose.gis.raster/irastervalues/
---

**Summary:** Provides access to the values of raster bands.

**Module:** [aspose.gis.raster](/psd/python-net/aspose.gis.raster/)

**Full Name:** aspose.gis.raster.IRasterValues

**Aspose.PSD Version:** 25.9.0

## **Methods**
| **Name** | **Description** |
| :- | :- |
| [as_boolean(band_index)](#as_boolean_band_index_1) | يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="bool" />. |
| [as_byte(band_index)](#as_byte_band_index_2) | يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="byte" />. |
| [as_double(band_index)](#as_double_band_index_3) | يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="double" />. |
| [as_float(band_index)](#as_float_band_index_4) | يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="float" />. |
| [as_integer(band_index)](#as_integer_band_index_5) | يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="int" />. |
| [as_long(band_index)](#as_long_band_index_6) | يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="long" />. |
| [as_short(band_index)](#as_short_band_index_7) | يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="short" />. |
| [equals_no_data(band_index)](#equals_no_data_band_index_8) | يتحقق مما إذا كانت القيمة تمثل الخلفية أو 'لا بيانات' في النطاق المحدد. |
| [get_data_type(band_index)](#get_data_type_band_index_9) | يحصل على نوع القيم. |
| [is_null(band_index)](#is_null_band_index_10) | يتحقق مما إذا كانت قيمة الراستر مضبوطة في النطاق المحدد. |


### Method: as_boolean(band_index) {#as_boolean_band_index_1}


```
 as_boolean(band_index) 
```

يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="bool" />.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| bool | القيمة المحوّلة. |


### Method: as_byte(band_index) {#as_byte_band_index_2}


```
 as_byte(band_index) 
```

يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="byte" />.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| byte | القيمة المحوّلة. |


### Method: as_double(band_index) {#as_double_band_index_3}


```
 as_double(band_index) 
```

يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="double" />.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| double | القيمة المحوّلة. |


### Method: as_float(band_index) {#as_float_band_index_4}


```
 as_float(band_index) 
```

يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="float" />.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| float | القيمة المحوّلة. |


### Method: as_integer(band_index) {#as_integer_band_index_5}


```
 as_integer(band_index) 
```

يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="int" />.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| int | القيمة المحوّلة. |


### Method: as_long(band_index) {#as_long_band_index_6}


```
 as_long(band_index) 
```

يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="long" />.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| long | القيمة المحوّلة. |


### Method: as_short(band_index) {#as_short_band_index_7}


```
 as_short(band_index) 
```

يقوم بتحويل بيانات النطاق المحدد إلى قيمة <see langword="short" />.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| short | القيمة المحوّلة. |


### Method: equals_no_data(band_index) {#equals_no_data_band_index_8}


```
 equals_no_data(band_index) 
```

يتحقق مما إذا كانت القيمة تمثل الخلفية أو 'لا بيانات' في النطاق المحدد.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| bool | إرجاع 'true' إذا كانت تمثل الخلفية أو 'لا بيانات'. |


### Method: get_data_type(band_index) {#get_data_type_band_index_9}


```
 get_data_type(band_index) 
```

يحصل على نوع القيم.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| [BandTypes](/psd/python-net/aspose.gis.raster/bandtypes) | نوع القيم. |


### Method: is_null(band_index) {#is_null_band_index_10}


```
 is_null(band_index) 
```

يتحقق مما إذا كانت قيمة الراستر مضبوطة في النطاق المحدد.

**Parameters:**

| معامل | نوع | وصف |
| :- | :- | :- |
| band_index | int | فهرس النطاق. يبدأ الترقيم من 0. |

**Returns**

| نوع | وصف |
| :- | :- |
| bool | إرجاع 'false' إذا لم توجد. |


