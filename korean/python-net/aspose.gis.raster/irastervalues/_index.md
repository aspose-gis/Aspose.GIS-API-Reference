---
title: "IRasterValues 클래스"
type: docs
weight: 30
url: /ko/python-net/aspose.gis.raster/irastervalues/
---

**Summary:** Provides access to the values of raster bands.

**Module:** [aspose.gis.raster](/psd/python-net/aspose.gis.raster/)

**Full Name:** aspose.gis.raster.IRasterValues

**Aspose.PSD Version:** 25.9.0

## **Methods**
| **Name** | **Description** |
| :- | :- |
| [as_boolean(band_index)](#as_boolean_band_index_1) | 지정된 밴드 데이터를 <see langword="bool" /> 값으로 변환합니다. |
| [as_byte(band_index)](#as_byte_band_index_2) | 지정된 밴드 데이터를 <see langword="byte" /> 값으로 변환합니다. |
| [as_double(band_index)](#as_double_band_index_3) | 지정된 밴드 데이터를 <see langword="double" /> 값으로 변환합니다. |
| [as_float(band_index)](#as_float_band_index_4) | 지정된 밴드 데이터를 <see langword="float" /> 값으로 변환합니다. |
| [as_integer(band_index)](#as_integer_band_index_5) | 지정된 밴드 데이터를 <see langword="int" /> 값으로 변환합니다. |
| [as_long(band_index)](#as_long_band_index_6) | 지정된 밴드 데이터를 <see langword="long" /> 값으로 변환합니다. |
| [as_short(band_index)](#as_short_band_index_7) | 지정된 밴드 데이터를 <see langword="short" /> 값으로 변환합니다. |
| [equals_no_data(band_index)](#equals_no_data_band_index_8) | 값이 지정된 밴드에서 배경 또는 'no data'를 나타내는지 확인합니다. |
| [get_data_type(band_index)](#get_data_type_band_index_9) | 값의 유형을 가져옵니다. |
| [is_null(band_index)](#is_null_band_index_10) | 지정된 밴드에 래스터 값이 설정되어 있는지 확인합니다. |


### Method: as_boolean(band_index) {#as_boolean_band_index_1}


```
 as_boolean(band_index) 
```

지정된 밴드 데이터를 <see langword="bool" /> 값으로 변환합니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| bool | 변환된 값입니다. |


### Method: as_byte(band_index) {#as_byte_band_index_2}


```
 as_byte(band_index) 
```

지정된 밴드 데이터를 <see langword="byte" /> 값으로 변환합니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| byte | 변환된 값입니다. |


### Method: as_double(band_index) {#as_double_band_index_3}


```
 as_double(band_index) 
```

지정된 밴드 데이터를 <see langword="double" /> 값으로 변환합니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| double | 변환된 값입니다. |


### Method: as_float(band_index) {#as_float_band_index_4}


```
 as_float(band_index) 
```

지정된 밴드 데이터를 <see langword="float" /> 값으로 변환합니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| float | 변환된 값입니다. |


### Method: as_integer(band_index) {#as_integer_band_index_5}


```
 as_integer(band_index) 
```

지정된 밴드 데이터를 <see langword="int" /> 값으로 변환합니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| int | 변환된 값입니다. |


### Method: as_long(band_index) {#as_long_band_index_6}


```
 as_long(band_index) 
```

지정된 밴드 데이터를 <see langword="long" /> 값으로 변환합니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| long | 변환된 값입니다. |


### Method: as_short(band_index) {#as_short_band_index_7}


```
 as_short(band_index) 
```

지정된 밴드 데이터를 <see langword="short" /> 값으로 변환합니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| short | 변환된 값입니다. |


### Method: equals_no_data(band_index) {#equals_no_data_band_index_8}


```
 equals_no_data(band_index) 
```

값이 지정된 밴드에서 배경 또는 'no data'를 나타내는지 확인합니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| bool | 배경 또는 'no data'를 나타내면 'true'를 반환합니다. |


### Method: get_data_type(band_index) {#get_data_type_band_index_9}


```
 get_data_type(band_index) 
```

값의 유형을 가져옵니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| [BandTypes](/psd/python-net/aspose.gis.raster/bandtypes) | 값의 유형입니다. |


### Method: is_null(band_index) {#is_null_band_index_10}


```
 is_null(band_index) 
```

지정된 밴드에 래스터 값이 설정되어 있는지 확인합니다.

**Parameters:**

| 매개변수 | 형식 | 설명 |
| :- | :- | :- |
| band_index | int | 밴드의 인덱스입니다. 번호는 0부터 시작합니다. |

**Returns**

| 형식 | 설명 |
| :- | :- |
| bool | 존재하지 않으면 'false'를 반환합니다. |


